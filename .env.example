# Personal access token Figma (Settings → Security → Personal access tokens).
# Нужен только на этапе разработки — для чтения макета DC-Final и выгрузки
# ассетов/референс-скринов (ТЗ, раздел 10). В гит не коммитится.
FIGMA_TOKEN=
